// Write a C program to get input an integer of three digits, and find the sum of the individual digits.
#include <stdio.h>
#include <stdlib.h>

int main()
{
    int x=5, y=4, z=1;
    int sum= x+y+z;
    printf("the sum is :%d \n",sum);

    return 0;
}
